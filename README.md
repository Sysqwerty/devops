# To start the docker compose run the following command

```bash
docker compose up -d
```

The web app is accessible at http://localhost:80

# To shut down the docker compose run the following command

```bash
docker compose down
```